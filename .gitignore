.DS_Store
*.backup.*

# Local MCP config (may contain API keys) — copy from .mcp.json.example
.mcp.json

# Active quarterly goals — sensitive (see canonical/GOALS.template.md for the starter)
/canonical/GOALS.md

# Personal weekly priorities tracker
/canonical/PRIORITIES.md

# Personal action items tracker
/canonical/action-items.md

# Sensitive data files (CSVs, notebooks may contain proprietary data)
canonical/data/*.csv
canonical/data/*.xlsx
canonical/data/*.ipynb
canonical/data/*.json
!canonical/data/README.md

